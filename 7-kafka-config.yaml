apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: kafka
  labels:
    app: kafka-config
data:
  KAFKA_ZOOKEEPER_CONNECT: zookeeper-svc:2181
  KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka-svc:9092
