apiVersion: v1
kind: Service
metadata:
  name: kafka-ui-svc
  namespace: kafka
  labels:
    app: kafka-ui
spec:
  type: NodePort
  ports:
  - name: client
    port: 8091
    targetPort: 8091
    nodePort: 30435
    protocol: TCP
  selector:
    app: kafka-ui
